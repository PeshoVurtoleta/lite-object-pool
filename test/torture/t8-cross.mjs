/**
 * T8 -- cross-package poison door. Empty in P0; filled in P3 (pool objects
 * flowing through a sibling @zakkster consumer without leaking or aliasing).
 */

export function run() {
    // Intentionally empty until P3.
}
